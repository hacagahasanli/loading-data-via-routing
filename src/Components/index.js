import { NotFound } from "./ErroPage";
import Posts, { postsLoader } from "./Posts";
import Post, { postLoader } from "./Post";
import Fallback from "./Fallback";
import Comments, { commentLoader } from "./Comments";

export { NotFound, Posts, postsLoader, Post, postLoader, Fallback, Comments, commentLoader };
